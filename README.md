# quizpro
Quiz Pro app for Tatpara Infolink Pvt Limited

**Create SQL Table**
```
CREATE TABLE myusers ( 
        userId VARCHAR(50), 
        username VARCHAR(50), 
        email VARCHAR(50), 
        password VARCHAR(50), 
        otp VARCHAR(10),  
        city VARCHAR(20) 
); 
```

**Insert Records**
```

```
